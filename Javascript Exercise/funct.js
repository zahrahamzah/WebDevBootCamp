
/* function testFunc(min, max){
    return function(num){
        return (num >= min) && (num < max);
    }
} */ // to comment use shift + alt + A

function upperCasing(hihi) {
    try {
        console.log(hihi.toUpperCase()); // why when I do return it's not working? hmm maybe cause return terus make it exit.
    } catch (e) {
        console.log("error yo");
    }
}

